import { Stack, Text } from "@chakra-ui/react";

const Aggregate = () => {
    return (
        <Stack p="10px">
            <Text>Aggregate</Text>
        </Stack>
    );
};

export default Aggregate;
